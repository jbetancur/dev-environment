kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

# Disable kindnet (default CNI) so Cilium can take over.
# Disable kube-proxy so Cilium can replace it with eBPF.
networking:
  disableDefaultCNI: true
  kubeProxyMode: none

nodes:
  - role: control-plane
  - role: worker
  - role: worker
